# 基于OpenHarmony的智能小车物联网系统设计文档

## 1. 系统架构设计

### 1.1 整体架构

本系统采用四层架构设计，从下至上分别为感知执行层、通信转换层、业务服务层和应用交互层。

```
┌─────────────────────────────────────────────────────────────────┐
│                      应用交互层                                   │
│  ┌─────────────────────┐     ┌─────────────────────┐           │
│  │      微信小程序       │     │     HAP应用          │           │
│  │  (Vant Weapp)       │     │  (ArkUI/HarmonyOS)   │           │
│  └──────────┬──────────┘     └──────────┬──────────┘           │
│             │                            │                      │
│             └────────────┬───────────────┘                      │
│                          │ UDP/TCP :8888                        │
│                          ▼                                      │
│  ┌───────────────────────────────────────────────────────┐      │
│  │                    Python后台服务                       │      │
│  │  UDP服务 │ 数据服务 │ 智能服务 │ 模拟服务               │      │
│  └───────────────────────┬───────────────────────────────┘      │
│                          │ UDP :7788                             │
│                          ▼                                      │
│  ┌───────────────────────────────────────────────────────┐      │
│  │              Hi3861 网关 (OpenHarmony)                 │      │
│  │  FreeRTOS: UDP/UART任务, 避障, 智能照明, OLED显示     │      │
│  └───────────────────────┬───────────────────────────────┘      │
│                          │ UART2 JSON :115200                    │
│                          ▼                                      │
│  ┌───────────────────────────────────────────────────────┐      │
│  │                 STM32 电机驱动板                        │      │
│  │  PID控制, 编码器反馈, 模式管理                         │      │
│  └───────────────────────┬───────────────────────────────┘      │
│                          │ UART2 Binary :115200                  │
│                          ▼                                      │
│  ┌───────────────────────────────────────────────────────┐      │
│  │                 STM32 传感器板                        │      │
│  │  超声波测距, 7路巡线, WS2812B RGB                     │      │
│  └───────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 硬件系统设计

#### 1.2.1 Hi3861网关板

| 参数 | 规格 |
|-----|------|
| 主芯片 | Hi3861V100 |
| 主频 | 160MHz |
| 内存 | 352KB SRAM |
| 通信接口 | WiFi, UART1, UART2, I2C0, GPIO |
| I2C设备 | AP3216C(0x3C), SHT20(0x80), AW2013(0x8A), PCF8574(0x43), SSD1306(0x78) |

**UART接口定义**
| UART | RX | TX | 用途 |
|-----|----|----|-----|
| UART1 | GPIO_5 | GPIO_6 | 语音模块 |
| UART2 | GPIO_12 | GPIO_11 | STM32通信 |

#### 1.2.2 STM32电机驱动板

| 参数 | 规格 |
|-----|------|
| 主芯片 | STM32G030F6Px |
| 主频 | 64MHz |
| 通信接口 | UART1, UART2, I2C |
| 编码器 | 528线, 1056脉冲/转 |
| 电机驱动 | PWM输出, 最小启动150(15%) |

#### 1.2.3 STM32传感器板

| 参数 | 规格 |
|-----|------|
| 主芯片 | STM32G030F6Px |
| ADC通道 | 7通道 (巡线传感器) |
| 传感器 | HC-SR04超声波, 光电探头×7 |
| LED驱动 | WS2812B RGB |

### 1.3 软件系统设计

#### 1.3.1 Hi3861 FreeRTOS任务

| 任务 | 优先级 | 周期 | 功能 |
|-----|-------|------|------|
| udp_recv_task | High | 事件触发 | 接收APP命令(UDP 7788) |
| udp_send_task | Medium | 50ms | 发送状态到APP |
| uart_recv_task | High | 事件触发 | 接收STM32数据 |
| uart1_recv_task | Normal | 事件触发 | 接收语音命令 |
| oled_show_task | Low | 100ms | OLED显示更新 |
| auto_avoid_task | Medium | 5ms | 超声波避障算法 |
| smart_light_task | Low | 100ms | 智能照明算法 |

#### 1.3.2 Python后台服务

```
backend/
├── main.py                      # 入口, SmartCarBackend类
├── config.py                    # 配置管理
├── app/
│   ├── __init__.py             # Flask应用工厂
│   ├── models/                  # SQLAlchemy模型
│   │   ├── device.py           # 设备模型
│   │   ├── sensor_data.py     # 传感器数据模型
│   │   ├── car_status.py       # 小车状态模型
│   │   ├── smart_light_status.py  # 智能照明模型
│   │   ├── irrigation_config.py    # 灌溉配置
│   │   └── irrigation_history.py  # 灌溉历史
│   └── services/
│       ├── udp_service.py      # UDP小车通信
│       ├── udp_miniapp_service.py  # UDP小程序通信
│       ├── data_service.py     # 数据存取
│       ├── smart_light_service.py  # 智能照明
│       ├── irrigation_service.py   # 智能灌溉
│       └── simulation_service.py   # 模拟数据
```

#### 1.3.3 客户端应用

**微信小程序页面结构**
```
pages/
├── home/                    # 首页 - 农业监测仪表盘
├── control/                 # 小车控制 - 摇杆/模式切换
├── environment/             # 环境监测 - 数据图表
├── smart_light/            # 智能照明
├── smart_irrigation/        # 智能灌溉
├── agriculture/             # 农业监测
├── monitor/                # 农业监控
├── path_planning/          # 路径规划
├── virtual_fence/          # 虚拟围栏
├── record_playback/         # 记录回放
├── nfc/                    # NFC配置
├── settings/               # 设置
└── backend_connect/        # 后端连接
```

**HAP应用 (HarmonyOS 6.0.0)**
- 使用ArkUI声明式开发框架
- 实现与微信小程序相同的功能
- 通过UDP Socket与后台通信

## 2. 通信协议设计

### 2.1 应用层 ↔ 后台

**通信方式**: UDP/TCP, 端口8888

**消息格式**: JSON

**客户端→后台消息类型**
| 类型 | 说明 |
|-----|------|
| ping | 心跳检测 |
| connect | 连接小车 |
| disconnect | 断开连接 |
| control | 控制命令 |
| demo_mode | 演示模式切换 |
| smart_light_* | 照明控制 |
| irrigation_calc | 灌溉计算 |

**后台→客户端消息类型**
| 类型 | 说明 |
|-----|------|
| pong | 心跳响应 |
| connect_result | 连接结果 |
| realtime | 实时数据推送 |
| control_result | 命令执行结果 |
| demo_mode_result | 演示模式结果 |
| smart_light_status | 照明状态 |
| irrigation_calc_result | 灌溉计算结果 |

### 2.2 后台 ↔ Hi3861

**通信方式**: UDP, 端口7788

**下发命令 (后台→Hi3861)**
```json
{"carStatus": "run"}              // 前进
{"carStatus": "back"}             // 后退
{"carStatus": "left"}             // 左转
{"carStatus": "right"}            // 右转
{"carStatus": "stop"}             // 停止
{"carMode": "avoid"}               // 避障模式
{"carSpeed": "middle"}             // 速度档位
{"joyX": 50, "joyY": 80}          // 摇杆
{"smartLight": {"mode": "auto", "brightness": 75}}  // 智能照明
```

**上报数据 (Hi3861→后台)**
```json
{"carStatus": "run", "L_spd": 100, "R_spd": 100, "carPower": 12000,
 "distance": 300, "carMode": "manual", "env": {"temp": 25, "humi": 60, "lux": 500}}
```

### 2.3 Hi3861 ↔ STM32电机板

**通信方式**: UART2, 115200, 紧凑JSON

**控制命令 (Hi3861→STM32)**
```json
{"control":{"turn":"run"}}
{"control":{"turn":"left","pwm":{"L_Motor":350,"R_Motor":350}}}
{"control":{"power":"on","mode":"avoid","speed":800}}
{"control":{"joyX":30,"joyY":100}}
```

**状态反馈 (STM32→Hi3861)**
```json
{"status":{"dist":150,"volt":12000,"L_spd":100,"R_spd":100,"mode":"manual"}}
```

### 2.4 STM32电机板 ↔ STM32传感器板

**通信方式**: UART2, Binary

**同步包 (电机板→传感器板)**: 8字节
| 字节 | 内容 |
|-----|------|
| 0 | 帧头 0xBB |
| 1-2 | 左轮速度 |
| 3-4 | 右轮速度 |
| 5 | 小车状态 |
| 6 | 运行模式 |
| 7 | 校验和 |

**避障数据包 (传感器板→电机板)**: 4字节
| 字节 | 内容 |
|-----|------|
| 0 | 帧头 0xFF |
| 1-2 | 距离值 |
| 3 | 校验和 |

**巡线数据包 (传感器板→电机板)**: 4字节
| 字节 | 内容 |
|-----|------|
| 0 | 帧头 0xAA |
| 1 | 巡线结果(7个光电管) |
| 2 | 模式字节 |
| 3 | 校验和 |

## 3. 功能模块设计

### 3.1 PID控制算法

电机板采用增量式PID算法进行速度闭环控制：

```c
struct PID {
    float Kp;     // 0.5
    float Ki;     // 0.15
    float Kd;     // 0.01
    float target; // 目标速度
    float actual; // 实际速度
};
```

**速度档位映射**
| 档位 | PWM | 速度(mm/s) |
|-----|-----|-----------|
| 低 | 390 | 500 |
| 中 | 420 | 800 |
| 高 | 450 | 1100 |

### 3.2 超声波避障算法

```c
void auto_avoid_task(void) {
    if (auto_abstacle_flag && car_status != CAR_STATUS_OFF) {
        // 手动优先窗口检查
        if ((now - g_lastManualTick) > MANUAL_OVERRIDE_MS) {
            if (distance <= MIN_DISTANCE_VOL) {  // 300mm
                // 后退
                uart_send_control_cmd(CAR_STATUS_BACK);
                osDelay(30);
                // 转向(左右交替)
                uart_send_control_cmd(g_turnToggle ? LEFT : RIGHT);
                g_turnToggle ^= 1;
            } else {
                uart_send_control_cmd(CAR_STATUS_RUN);
            }
        }
    }
}
```

### 3.3 智能照明算法

**光照等级划分**
| 等级 | Lux范围 | 基础亮度 |
|-----|--------|---------|
| 暗 | <50 | 100% |
| 昏暗 | 50-200 | 75% |
| 低 | 200-500 | 50% |
| 正常 | 500-1000 | 30% |
| 明亮 | 1000-2000 | 15% |
| 强光 | >2000 | 0% |

**亮度计算**: 最终亮度 = 基础亮度 × 时间因子

### 3.4 智能灌溉算法

```python
def calculate_water_amount(self, temperature, humidity, lux, device_id):
    base = self.config.base_water_amount  # 500ml

    # 各因子计算
    temp_factor = 1.0 + (temperature - 25) * 0.02
    humi_factor = 1.0 + (60 - humidity) * 0.015
    light_factor = 1.0 + (lux / 10000) * 0.1

    # 历史因子检查
    recent = self.get_recent_irrigation(device_id)
    history_factor = 0.5 if recent else 1.0

    # 季节因子
    season = self.get_current_season()
    season_factor = {'spring': 1.1, 'summer': 1.3, 'autumn': 1.0, 'winter': 0.7}[season]

    water_amount = base * temp_factor * humi_factor * light_factor * history_factor * season_factor
    return max(100, min(2000, water_amount))  # 限制在100-2000ml
```

## 4. 数据库设计

### 4.1 数据表

| 表名 | 说明 |
|-----|------|
| devices | 设备注册表 |
| sensor_data | 传感器数据 |
| car_status | 小车状态 |
| smart_light_status | 智能照明状态 |
| irrigation_config | 灌溉配置 |
| irrigation_history | 灌溉历史 |
| control_commands | 控制命令日志 |
| simulated_sensor_data | 模拟传感器数据 |
| car_sensor_data | 小车传感器数据 |

### 4.2 索引设计

- device_id + timestamp 联合索引 (查询特定设备历史)
- timestamp 单独索引 (按时间范围查询)
- is_simulated 索引 (区分模拟/实际数据)

## 5. 接口设计

### 5.1 后台UDP服务接口

```python
class UDPService:
    def send_command(self, command: dict) -> bool
    def receive_data(self) -> dict
    def start_heartbeat(self, interval: int = 3)
    def stop(self)

class UDPMiniAppService:
    def start(self) -> bool
    def broadcast_realtime_data(self, data: dict)
    def handle_message(self, message: dict)
```

### 5.2 数据服务接口

```python
class DataService:
    def save_sensor_data(self, device_id: str, data: dict) -> SensorData
    def save_car_data(self, device_id: str, data: dict) -> CarSensorData
    def save_control_command(self, device_id: str, command_type: str,
                            command_data: dict, source: str) -> ControlCommand
    def query_history(self, device_id: str, hours: int, data_type: str) -> dict
    def get_statistics(self, device_id: str, hours: int) -> dict
```

## 6. 安全设计

### 6.1 命令优先级

| 优先级 | 命令类型 |
|-------|---------|
| 最高 | 手动控制(摇杆/按钮) |
| 高 | 模式切换 |
| 中 | 自动控制 |
| 低 | 查询命令 |

### 6.2 互锁机制

- 模式切换后500ms内忽略物理按键冗余请求
- 避障执行中忽略新的避障指令
- 电机启动需满足最小PWM要求(150)

## 7. 性能参数

| 参数 | 值 | 说明 |
|-----|-----|------|
| 控制响应时间 | ≤100ms | 指令到电机响应 |
| UDP发送间隔 | 50ms | Hi3861状态上报 |
| 数据存储间隔 | 1s | 后台数据库保存 |
| 最小启动PWM | 150 | 15%占空比 |
| 90°转向脉冲 | 920 | 编码器脉冲数 |
| 超声波测距周期 | 50ms | 采样周期 |
| 巡线检测周期 | 20ms | ADC采样 |
| OLED刷新周期 | 100ms | 显示更新 |
| 避障检测周期 | 5ms | 超声波检测 |
